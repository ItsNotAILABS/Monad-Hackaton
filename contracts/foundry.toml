[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.26"
optimizer = true
optimizer_runs = 200
metadata = true
metadata_hash = "none"
use_literal_content = true
eth-rpc-url = "${MONAD_RPC_URL}"

[rpc_endpoints]
monad = "${MONAD_RPC_URL}"
