[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "thesis-forge"
version = "0.4.1"
description = "THESIS — Monad AI Workstation (Studio · Codex · Nomos · Desk · Academy)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "pydantic>=2.8",
]

[project.optional-dependencies]
dev = ["pytest>=8.2", "httpx>=0.27"]

[tool.setuptools.packages.find]
include = ["thesis_forge*"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
