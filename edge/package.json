{
  "name": "monadbuilder-edge-agents",
  "version": "3.2.0",
  "private": true,
  "description": "Cloudflare Workers — edge AI agents for MonadBuilder HQ",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
